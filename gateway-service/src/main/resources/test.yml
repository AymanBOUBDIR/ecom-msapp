spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
           - id : R1
             uri: lb://CUSTOMER-SERVICE
             predicates:
               - Path= /customers/**
           - id : R2
             uri: lb://INVENTORY-SERVICE
             predicates:
               - Path= /products/**
